Calm, encouraging empty view — icon in a glass circle, headline, one supportive line, optional action. Never blames. Requires Lucide loaded.

```jsx
<EmptyState icon="receipt" title="No transactions yet"
  message="Add your first — camera, voice, or by hand."
  action={<Button>Add expense</Button>} />
```
