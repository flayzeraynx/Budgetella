Budgetella button — solid violet primary, bordered-glass secondary, text-only ghost.

```jsx
<Button>Save</Button>
<Button variant="secondary" full>Maybe later</Button>
<Button variant="ghost" size="sm">Cancel</Button>
```

Variants: `primary` (default, accent glow) · `secondary` · `ghost`. Sizes: `sm` · `md` · `lg`. `full` for full width. Press = scale 0.97, smooth release.
