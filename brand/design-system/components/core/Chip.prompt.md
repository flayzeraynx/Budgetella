Small rounded label — category filter (with a leading color dot), tag, or eyebrow pill.

```jsx
<Chip dotColor="#10F2A5">Freelance</Chip>
<Chip active onClick={…}>All</Chip>
```

`active` gives the filled accent state. Add `dotColor` for the leading category dot.
